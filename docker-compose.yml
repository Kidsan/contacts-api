version: '3'

services:
  database:
    image: postgres:13
    environment:
      POSTGRES_DB: "contacts_api"
      POSTGRES_USER: "contactsapi"
      POSTGRES_PASSWORD: "contactsapi"
    ports:
      - 5432:5432